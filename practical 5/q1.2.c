#include <stdio.h>
#include <stdlib.h>
int main()
{
int no;
//the no is checked whether its less than or equal to 100 then printed and after that incremented
//by one. the loop iterates until the condition remain true
for(no=0;no<=100;no++)
{
printf("%d \n",no);
}
return 0;
}

#include <stdio.h>
#include <stdlib.h>
int main()
{
int no=0;
// here first the number is printed and then the condition is checked

do
{
printf("%d \n",no);
no=no+1;
} while(no<=100);
return 0;
}
